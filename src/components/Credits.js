import React from 'react';
import styled from 'styled-components';

const CreditsWrapper = styled.div`
`;

const Credits = () => {

    return (
        <CreditsWrapper></CreditsWrapper>
    );
};

export default Credits;