import React from 'react';
import styled from 'styled-components';

const PlaybarWrapper = styled.div`
`;

const Playbar = () => {

    return (
        <PlaybarWrapper></PlaybarWrapper>
    );
}

export default Playbar;